import styled from 'styled-components';

export const ItemStyle = styled.div`
  float: left;
  height: max-content;

  label {
    font-weight: 500;
    color: #03B7DA;
  }

  input {
    margin-top: 5px;
  }

  input, label {
    display: block;
  }
`;
